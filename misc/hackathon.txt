(0) GET DATA

get clips.tsv
get audio data


(1) GET DEEPSPEECH + VENV

git clone https://github.com/mozilla/DeepSpeech
cd DeepSpeech
bash .install # we need the venv to work for CorporaCreator
source ../tmp/venv/bin/activate
cd ..


(2) GET DEEPSPEECH + FORMAT TSVs

git clone https://github.com/mozilla/CorporaCreator
cd CorporaCreator
sudo python3 setup.py install
create-corpora -f clips.tsv -d corpora -l ky -s 10
cd ..


(3) TRAIN DEEPSPEECH

cd DeepSpeech
python3 bin/import_cv2.py ../CorporaCreator/corpora/ky ~/Downloads/ky
emacs .compute # add paths to ~/Downloads/ky/{train,dev,test}.csv
bash .compute
